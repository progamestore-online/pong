{
  "name": "pong",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @pong/web dev",
    "build": "pnpm --filter @pong/web build",
    "preview": "pnpm --filter @pong/web preview",
    "typecheck": "pnpm --filter @pong/web exec tsc -b",
    "test": "pnpm --filter @pong/web test",
    "deploy": "wrangler deploy"
  }
}
